import { StyleSheet } from "react-native";

export const plantStyles = StyleSheet.create({
    input: {
        height: 48,
        margin: 12,
        borderWidth: 1,
        padding: 10,
        borderRadius: 10,
    },
});